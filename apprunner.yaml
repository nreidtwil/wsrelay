version: 1.0
runtime: go1
build:
  commands:
    build:
      - go build -o wsrelay .
run:
  runtime-version: go1
  command: ./wsrelay
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
